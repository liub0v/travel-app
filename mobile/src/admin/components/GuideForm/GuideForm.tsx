import React from 'react';

type Props = {
  adventure: any;
  editHandler: any;
  setImage: any;
  isLoading: boolean;
};

export const GuideForm: React.FC<Props> = ({
  adventure,
  editHandler,
  setImage,
  isLoading,
}) => {
  return null;
};
